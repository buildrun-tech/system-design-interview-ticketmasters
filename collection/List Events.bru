meta {
  name: List Events
  type: http
  seq: 6
}

get {
  url: http://localhost:8080/events
  body: none
  auth: bearer
}

params:query {
  ~pageSize: 1
  ~page: 3
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
