meta {
  name: Create Event
  type: http
  seq: 8
}

post {
  url: {{api-url}}/events
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Final da Libertadores",
    "description": "FIFA",
    "settings": {
      "numberOfSeats": 5
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
