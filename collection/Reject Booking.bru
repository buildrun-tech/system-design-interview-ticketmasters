meta {
  name: Reject Booking
  type: http
  seq: 15
}

post {
  url: http://localhost:8080/bookings/reject
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "bookingId": 1
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
