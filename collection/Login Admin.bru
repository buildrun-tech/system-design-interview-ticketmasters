meta {
  name: Login Admin
  type: http
  seq: 5
}

post {
  url: {{api-url}}/auth/token
  body: json
  auth: inherit
}

body:json {
  {
    "grantType": "password",
  "username": "admin",
  "password": "admin"
  }
}

vars:post-response {
  token: res.body.accessToken
}

settings {
  encodeUrl: true
  timeout: 0
}
