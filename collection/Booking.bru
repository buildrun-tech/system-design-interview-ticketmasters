meta {
  name: Booking
  type: http
  seq: 12
}

post {
  url: {{api-url}}/bookings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "eventId": 1,
    "seats": [
      {
        "seatId": 1
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
