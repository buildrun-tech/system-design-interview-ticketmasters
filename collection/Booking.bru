meta {
  name: Booking
  type: http
  seq: 12
}

post {
  url: http://localhost:8080/bookings
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "eventId": 1,
    "seats": [
      {
        "seatId": 3
      }
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
