meta {
  name: Confirm Booking
  type: http
  seq: 13
}

post {
  url: {{api-url}}/bookings/confirm
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "bookingId": 3
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
