meta {
  name: Login User
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/auth/token
  body: json
  auth: inherit
}

body:json {
  {
    "grantType": "password",
    "username": "bruno",
    "password": "1234"
  }
}

vars:post-response {
  token: res.body.accessToken
}

settings {
  encodeUrl: true
  timeout: 0
}
