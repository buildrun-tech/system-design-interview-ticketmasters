meta {
  name: Create App
  type: http
  seq: 14
}

post {
  url: http://localhost:8080/apps
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "payment-gateway",
    "scopes": [
      "bookings:confirm",
      "bookings:reject"
    ]
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
